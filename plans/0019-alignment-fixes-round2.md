# 0019 — 정렬 재수정: 부동소수점 표시 + 한글 폭 가정 되돌림

**날짜**: 2026-07-15

**대상 작업**: 사용자가 실제 화면에서 발견한 두 가지 정렬 문제 수정.
1. 생산 라인 메뉴의 "총(분)" 컬럼에 `0.6000000000000001` 같은 부동소수점 오차가 그대로 노출되어 자릿수가 들쭉날쭉함.
2. 모니터링 재고량 확인 표가 여전히 안 맞는다는 피드백 — 0015에서 넣은 "한글은 2칸 폭" 가정이 실제 사용 중인 콘솔 환경과 맞지 않아 오히려 정렬을 깨뜨리고 있었음.

**수정**:
- `console_view.py`의 생산 라인 메뉴에서 경과/총/남은(분) 값을 `f"{value:.2f}"`로 고정 포맷.
- `table.py`의 `visible_len`에서 `unicodedata.east_asian_width` 기반 2칸 폭 계산을 제거하고 순수 문자 개수로 되돌림 — 대상 콘솔이 한글을 1칸 폭으로 렌더링하는 것으로 확인됨.
- 관련 테스트(`test_table.py`)를 문자 개수 기준으로 갱신.

**상태**: ACTION 완료 (전체 96개 테스트 통과, 재현 스크립트로 두 문제 모두 재확인). COMMIT 완료 — `8c13e59`, `874ed21`.
