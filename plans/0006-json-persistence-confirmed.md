# 0006 — 확인: 영속성 방식은 JSON 파일로 확정

**날짜**: 2026-07-15

**대상 작업**: PRD.md 6절(범위 밖)의 "실제 DB 연동" 항목에 모호하게 남아있던 "JSON 파일 등" 표현을 JSON 파일 확정으로 명확화.

**EXPLORE에서 확인한 사실/제약**:
- CLAUDE.md/PRD.md 5절에는 이미 JSON 파일 영속성이 기본값으로 기술돼 있었으나, "등"이라는 표현으로 다른 방식(SQLite 등) 여지가 열려 있었음.
- DataPersistence PoC가 JSON 파일 CRUD로 이미 구현된 선례.

**결정**: 사용자 확정 — 실제 DB 없이 JSON 파일로 저장/처리. RDB/NoSQL 등 외부 DB 서버 연동 없음.

**실행 계획**:
1. PRD.md 5절(비기능요구사항), 6절(범위 밖), 8.1절(확정된 결정)에 JSON 파일 확정을 명시.

**상태**: COMMIT 완료 (`45b9328`).
